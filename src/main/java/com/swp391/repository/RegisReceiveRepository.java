package com.swp391.repository;

import com.swp391.entity.RegisReceive;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RegisReceiveRepository extends JpaRepository<RegisReceive, Integer> {
}
